import {Paper, TableContainer, TableHead, TableRow} from "@mui/material";

function Table() {
    return (
        <TableContainer component={Paper}>
            <Table>
                <TableHead>
                    <TableRow>
                        
                    </TableRow>
                </TableHead>
            </Table>
        </TableContainer>
    )
}